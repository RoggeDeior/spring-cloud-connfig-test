server:
  port: 8092

eureka:
  client:
    security:
      basic:
        user: admin
        password: admin
    service-url:
      defaultZone: http://${eureka.client.security.basic.user}:${eureka.client.security.basic.password}@localhost:8091/eureka/
